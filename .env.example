# 环境变量示例文件
# 复制此文件为 .env 并填入实际值

# 服务器端口（可选，默认 3001）
SERVER_PORT=3001

# AI 配置（必填）
AI_API_KEY=your-api-key-here
AI_ENDPOINT=https://api.deepseek.com/v1/chat/completions
AI_MODEL=deepseek-chat
AI_TEMPERATURE=0.8

# 注意：如果不配置环境变量，将使用 config/config.ts 中的默认值
